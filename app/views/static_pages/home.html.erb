<% provide(:title, 'Home') %>
<div>
    <% @merc.each do |merc| %>

        <%= timeline_container("#{merc.pagemap.metatags[0]["og:title"]}", merc.pagemap.metatags[0].startdate.to_date, 'sanjose.png') do %>
                        <div>
                            By Hannah Knowles on <%= merc.pagemap.metatags[0].startdate.to_date.strftime("%B %d, %Y") %>
                        </div>
            <h4 class="padded_bot"><%= link_to "Read More", merc.link, class: 'btn btn-sm btn-info' %></h4>


                    <div class="padded_san">
                           <%= image_tag 'sanjose.png'%>
                       </div>
            <% end %>

        <% end%>
<div>

